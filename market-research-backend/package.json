{
  "name": "kthq-research-desk",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test ../tests/market-research.test.mjs", "dev": "wrangler dev", "deploy": "wrangler deploy --keep-vars", "check": "wrangler deploy --dry-run" },
  "devDependencies": { "wrangler": "4.130.0", "@cloudflare/workers-types": "latest" }
}
